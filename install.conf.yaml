- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.gitconfig: .gitconfig
    ~/.config/niri: niri

- omnipkg:
    - install: [
        helium-browser-bin,
        zed,
        mise,
        sbctl,
        ghostty]

- shell:
  - [git submodule update --init --recursive, Installing submodules]
