- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.gitconfig: .gitconfig
    ~/.config/zellij:
      path: zellij

- shell:
  - [git submodule update --init --recursive, Installing submodules]
